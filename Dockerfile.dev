# Dockerfile.dev for NestJS API with hot reload
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
RUN apk add --no-cache sqlite
EXPOSE 3000
CMD ["npm", "run", "start:dev"]
